<%layout("/layouts/boilerPlate") %>

    <body>



        <div class="col-6 offset-3 mt-4">
   
            <% if(data.length == 0){ %>
 
                <h2 class="offset-3">No Results Found😕</h2>

            <% } %>

            <% for(farms of data){ %>

                <a href="/listings/<%=farms._id%>" class="listing-link">

                    <div class="card listing-card ">
                        <div class="card-img-overlay"></div>
                        <img src="<%=farms.image.url%>" class="card-img-top " style="height: 18rem;"
                            alt="<%=farms.image%>">
                        <div class="card-body">
                            <p class="card-text">
                                <b>
                                    <p>
                                        <%=farms.title%>
                                    </p>
                                </b>
                            <p>&#36 <%=farms.price.toLocaleString("en-US")%></p>
                            </p>
                        </div>
                    </div>
                </a>


       

        <% } %>

    </div>

    </body>